<div class="container mt-5">
	<div class="row">
		<div class="col-6 mx-auto">
			<div class="card">
				<div class="card-body">
					<h5 class="card-title"><%= @recipe.title %></h5> <!-- この@recipeはshoqアクションの@recipeとなる。つまり１つ分のレコードを取ってきており、その情報が@recipeに入っている。 -->
					<p class="card-text"><%= simple_format @recipe.body %></p>　<!-- simple_formatと入れることでbodyの中で改行が反映される。 -->
					<p class="card-text">
						<small class="text-muted">更新日：<%= @recipe.updated_at.strftime("%Y-%m-%d") %></small>
						<% if @recipe.user == current_user %>
							<%= link_to '編集', edit_recipe_path(@recipe), class: "btn btn-success" %>
							<%= link_to '削除', recipe_path(@recipe), method: :delete, class: "btn btn-danger" %><!-- どのレシピを編集、削除するのかが分からないといけないので引数に(@recipe)を入れる -->
						<% end %>
					</p>
				</div>
			</div>
		</div>
	</div>
</div>
